# AGENTS.md

# 浮光相机（Shimmer Camera）

## 项目简介

浮光相机是一款 Android 临时照片管理应用。

核心理念：

> 让不需要永久保存的照片拥有独立空间，并自动管理照片生命周期。

浮光相机不是一个专业摄影工具，而是解决手机相册长期堆积大量临时照片的问题。

用户可以使用浮光相机拍摄：

- 临时购物参考照片
- 工作记录照片
- 快递、票据照片
- 旅行辅助照片
- 短期查看后无需长期保存的图片

这些照片：

- 不进入系统相册
- 存储在 APP 独立目录
- 默认保存 30 天
- 到期后自动清理

---

# 工作约定

- 禁止在终端运行构建、安装类命令（如 `gradlew`、`./gradlew`、`adb install` 等）。
- 代码修改完成后，不要尝试自行编译或运行验证；提示用户在 Android Studio 中执行即可。

---

# 当前进度

已完成拍摄功能：屏幕下方圆形快门按钮，点击拍摄（CameraX 相机预览 + 拍摄）；
照片保存到应用专属目录（`Android/data/com.example.shimmer/files/Pictures`），
目录内放置 `.nomedia` 文件，不会进入系统相册。
交互流程：启动进入相册页（三列网格展示已拍照片），底部为白色矩形控制条（128dp）；
点击圆钮切换到摄影页，再点拍摄；系统返回键退回相册页。
相册页快门键为青色全实心圆 + 白色十字加号（点击进入拍摄页），拍摄页为外圈 + 实心圆（点击拍摄）。
相册页底部白条只覆盖加号键中心线以下的 64dp（加号键位置大小不变），
左右各为纯文字“调整”和“设置”；“调整”弹窗为“- 数字 +”胶囊步进器
（每行 2-6 张）+ “相机/截屏”两个独立开关（默认都选中，截图以 SCR_ 前缀区分），
查看器跟随筛选；两者都关闭时显示“没有符合筛选条件的照片”；
拍摄页底部保持 128dp 白条与胶囊按钮。
整体背景为白色，底部控制条为白色，快门按钮为青色（#00BCD4），
控制条左侧为相册入口（仅摄影页显示）：“相册”文字胶囊按钮（浅青底青字，与查看页保存/删除同款），
点击回到相册页；控制条右侧为保存天数胶囊按钮（显示“X天”，同款样式），
点击弹出居中选择弹窗（1/7/30 天），每档注明到期凌晨，默认 30 天。
相册页每张照片顶部显示灰色半透明条 + “剩余X天”（今天算 1 天，到期当天为 0）。
相册内容区统一为浅灰白（#F5F5F5，空相册与有照片时一致），底部控制条为纯白，便于区分两个区域。
相册网格内容避开系统状态栏（背景色仍铺满），照片不会顶到状态栏下面。
相册目录有文件监听（FileObserver）：出现新文件立即刷新列表；同时保留回前台刷新兜底。
截图/拍摄后无需任何操作即可看到新图片（仅刷新，无清理逻辑）。
应用图标已替换：新版 logo.png 生成各密度 PNG（ic_launcher / ic_launcher_round），
图标改用标准自适应结构：back.jpg（白底）作背景满幅显示，front.png（主体）作前景；
前景主体按画布 75% 缩放居中，避免被遮罩裁切（主体约 60% 宽 / 48% 高）；
Android 8+ 用自适应图标，低版本用 back+front 合成的各密度 PNG。根目录素材文件保留。
相册页点击照片全屏查看：图片全尺寸展示，左右滑动切换照片；
白色顶栏（青色绘制返回箭头 + 居中拍摄时间）和白色底栏（青色“保存”/“删除”按钮）
悬浮盖在图片上，点图片收起/展开两栏，返回键或箭头退出；
保存到手机相册（Android 10+ 免权限 / 低版本请求存储权限），
删除直接从应用目录移除并自动切到下一张，删除最后一张时退出查看。
照片有效期自动清理：拍摄时按当前选择的有效期（1/7/30 天，默认 30 天）写入文件名；
WorkManager 每天凌晨 2 点周期清理过期照片（当天不计入天数，如选 1 天则后天凌晨删除），
无启动时清理逻辑；旧版本无后缀照片按默认 30 天处理。
截图功能：快捷设置栏磁贴“浮光截图”，截图直接保存到 APP 目录（沿用有效期命名规则，
受凌晨清理任务管理），系统相册不产生副本、无需删除权限。
相册页快门键右侧为“设置”按钮（胶囊样式），进入设置页：
- 默认保存时长（点击弹出保存时长选择弹窗）
- 无障碍截图开关：开启需先在系统设置开启“浮光截图服务”（Android 11+），
  开启后点击磁贴静默截图；关闭则回到“每次授权”模式（点击磁贴弹一次系统授权）
待真机/模拟器运行验证。

---

# 当前目标

只完成拍摄功能，屏幕下方有圆形按钮，点击可拍摄，图片存放在APP文件夹下，不进入系统相册。
